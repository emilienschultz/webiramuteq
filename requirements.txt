# Dépendances Python d'IRaMuTeQ 0.8 alpha 7 (Python 3)
#
#   pip install -r requirements.txt
# ou : ./install_deps.sh  (installe aussi les paquets R)

# --- interface graphique (iramuteq.py) et ligne de commande (iracmd.py) ---
wxPython>=4.1
xlrd>=2.0

# --- explorateur web (iraexplorer/) ---
streamlit>=1.26
pandas>=1.5
altair>=5.0
